# Only needed to override the default proxy-based endpoint (window.location.origin + '/api').
# In Docker and dev, the endpoint is resolved automatically via nginx/Vite proxy.
# VITE_FLOCI_ENDPOINT=http://localhost:4566
